numpy==1.26.0  # Latest version compatible with Python 3.12
pandas==1.5.3  # Updated to a version compatible with numpy
plotly==5.14.0  # Latest version compatible with the previous packages
pmdarima==2.0.4  # Existing version seems compatible, check if you need an update
scikit-learn==1.3.0  # Updated to a version compatible with Python 3.12
tensorflow==2.13.0  # Downgrade to a compatible version
st_pages==1.0.1
statsmodels==0.13.2
streamlit==1.37.1
python==3.11.0
